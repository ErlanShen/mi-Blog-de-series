import { ConfirmadorDirective } from './confirmador.directive';

describe('ConfirmadorDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmadorDirective();
    expect(directive).toBeTruthy();
  });
});
